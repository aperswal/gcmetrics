# PATH used by the daily job. launchd starts with almost no PATH, so list where uv, node, pnpm, vercel and git live.
export PATH="$HOME/.local/bin:$HOME/Library/pnpm:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"
